import React from "react";
import DesvincularCredencial from "../../components/pages/DesvincularCredencial";

export default {
  title: "LANA/Páginas/DesvincularCredencial",
  component: DesvincularCredencial,
};

const Template = () => <DesvincularCredencial />;

export const DesvincularCredencialTradicional = Template.bind({});